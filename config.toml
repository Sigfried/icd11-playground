# ICD-11 API Configuration

[api]
server = "docker"  # Options: "docker", "official"
version = "v2"
language = "en"
release = "2024-01"

[servers]
docker = "http://localhost:80"
official = "https://id.who.int"
